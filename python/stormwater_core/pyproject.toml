[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "stormwater-core"
version = "0.1.0"
description = "Reusable Stormwater V2 domain, photo, Drive, and report logic."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]

