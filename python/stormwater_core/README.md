# stormwater_core

Reusable Python package for Streamlit-free stormwater domain, photo, Drive naming,
and report rendering logic.

Extraction rule: only move code here when it does not import Streamlit, page
modules, sidebar/UI modules, or `st.session_state`.

